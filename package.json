{
  "name": "fullstack-playground",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "docker:dev": "docker-compose -f docker-compose.dev.yml --env-file .development.env",
    "start": "npm run docker:dev up -d",
    "stop": "npm run docker:dev stop",
    "restart": "npm stop && npm run docker:dev build -- --no-cache && npm run docker:dev up -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}